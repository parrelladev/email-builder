{
  "name": "@usewaypoint-monorepo",
  "version": "0.0.6",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "cd packages/editor-sample && npm run dev",
    "build": "npm install && npm run build:packages && cd packages/editor-sample && npm install && npm run build",
    "build:packages": "npm run build:core && npm run build:blocks && npm run build:builder",
    "build:core": "cd packages/document-core && npm install && npm run build",
    "build:blocks": "npm-run-all --parallel build:block:*",
    "build:block:avatar": "cd packages/block-avatar && npm install && npm run build",
    "build:block:button": "cd packages/block-button && npm install && npm run build",
    "build:block:columns": "cd packages/block-columns-container && npm install && npm run build",
    "build:block:container": "cd packages/block-container && npm install && npm run build",
    "build:block:divider": "cd packages/block-divider && npm install && npm run build",
    "build:block:heading": "cd packages/block-heading && npm install && npm run build",
    "build:block:html": "cd packages/block-html && npm install && npm run build",
    "build:block:image": "cd packages/block-image && npm install && npm run build",
    "build:block:spacer": "cd packages/block-spacer && npm install && npm run build",
    "build:block:text": "cd packages/block-text && npm install && npm run build",
    "build:builder": "cd packages/email-builder && npm install && npm run build",
    "test": "npx jest"
  },
  "author": "carlos@usewaypoint.com",
  "license": "MIT",
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@testing-library/react": "^14.2.1",
    "@types/jest": "^29.5.12",
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "eslint": "^8.56.0",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-simple-import-sort": "^11.0.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.2.5",
    "react-test-renderer": "^18.2.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "tsup": "^8.0.2",
    "typescript": "^5.3.3",
    "zod": "^3.22.4"
  },
  "dependencies": {
    "@mui/material": "^5.15.11",
    "@mui/icons-material": "^5.15.11",
    "react-dropzone": "^14.2.3",
    "highlight.js": "^11.9.0",
    "marked": "^12.0.0",
    "insane": "^2.6.2",
    "zustand": "^4.5.1",
    "@vitejs/plugin-react-swc": "^3.6.0"
  }
}
